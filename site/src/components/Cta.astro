<section class="cta">
  <div class="inner">
    <h2>Your models should follow you between agents.</h2>
    <p>Start with native OpenCode variants, automatic Pi and Hermes setup, or any local client that accepts a custom OpenAI endpoint.</p>
    <div class="actions">
      <a class="btn primary" href="#install">Get started</a>
      <a class="btn ghost" href="https://github.com/designxdevelop/model-ferry" target="_blank" rel="noopener">Star on GitHub</a>
    </div>
  </div>
</section>

<style>
  .cta { padding: 0 24px 96px; }
  .inner {
    max-width: 1120px; margin: 0 auto; text-align: center;
    border: 1px solid var(--line); border-radius: 20px; background:
      radial-gradient(600px 260px at 50% 0%, rgba(47, 129, 247, 0.12), transparent 70%), var(--bg-soft);
    padding: 72px 24px;
  }
  h2 { font-size: clamp(28px, 4vw, 42px); }
  .inner p { color: var(--muted); font-size: 17px; margin: 14px 0 32px; }
  .actions { display: flex; justify-content: center; gap: 12px; flex-wrap: wrap; }
</style>
