{
  "name": "model-ferry-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.0",
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.120.0"
  }
}
